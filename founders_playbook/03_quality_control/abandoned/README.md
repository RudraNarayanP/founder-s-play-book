# abandoned/ — partial artefacts kept, not deleted

Method §14 rule 4 forbids deleting another pass's work, so files that are superseded or stranded are moved
here with their provenance instead. Nothing in this directory is evidence: cite the top-level sheet of the
same name.

**`amazon_s2_audit4_repairs.KILLED-PARTIAL-copy.md`** — written 22:57 by a replacement pass I dispatched by
mistake while the original pass on the same job was still alive (the first orchestrator-caused double-dispatch
since §14.7 was written). I terminated that agent mid-run; this is the skeleton it had created, in the wrong
directory (`company_001_amazon/03_quality_control/`, which it appears to have created). The authoritative
repair log is `../amazon_s2_audit4_repairs.md` (45 KB, plus the dated CORRECTION section the completed pass
appended). Kept because it is the audit trail of my own dispatch error, recorded as RD-082.
