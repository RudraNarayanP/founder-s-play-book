# _MANIFEST.md — company_042_target

_Regenerated by the Stage-1 merge pass, 2026-09-26 (agent `target-s1-merge`). Word counts live
(method §9.7). Hard cap 60,000 words per file (§9.2); amber 40,000–60,000 is allowed and is never cured by
trimming evidence. Nothing in this directory was trimmed to fit a limit._

| File | Words | Bytes | Sections contained | Upload batch | Status |
|---|---|---|---|---|---|
| `stage_1.md` | 37,507 | 255,104 | merge header + merge note, then Volume 1 (Header, Boundary, §A–§H) and Volume 2 (§I–§U, claim records, the three register-emission blocks) — **one volume**, §9.3 split not triggered | 5 (companies 041–050) | WITHIN CAP (below the 40,000 soft target) |
| `stage_1_index.md` | 850 | 5,834 | volume list, word counts, anchor ranges, register counts, the six merge decisions, the retired-id map | 5 | WITHIN CAP |
| `CORRECTIONS.md` | 1,246 | 8,140 | **created 2026-09-26 by the merge pass** — COR-01 (fiscal ≠ calendar, re-bases B1's Q20), COR-02 (the 1972-03-22 floor that greps to zero), COR-03 (Brookdale/Roseville mis-citation trap), COR-04 (the §G site-tenure retraction), COR-05 (FY1970 byte count 52,736 → 53,023), COR-06 (eight column-drift repairs, no value altered); each names the register rows and the volume it reaches | 5 | WITHIN CAP |
| `sources.csv` | 2,698 | 22,693 | 21 rows × 18 — append-only provenance register, global `S4201`–`S4221`, retired local ids kept as aliases in `notes` | 5 | WITHIN CAP |
| `quantitative.csv` | 3,397 | 27,652 | 61 rows × 12 — every DERIVED/ESTIMATE row carries `derived_arithmetic`; **all 61 rows carry a CONTEMPORANEOUS / RESTATED / MIXED / NOT-APPLICABLE period basis** (37 tags added at merge under COR-01) | 5 | WITHIN CAP |
| `timeline.csv` | 1,084 | 8,743 | 24 rows × 11 — chronology, `source_id` re-pointed to global ids | 5 | WITHIN CAP |
| `conflicts.csv` | 2,735 | 19,603 | 18 rows × 15 — 16 keyed to the declared §U anchors + K16/K17; every row opens with a printed collision note naming its pre-merge local keys | 5 | WITHIN CAP |
| `data_gaps.csv` | 1,967 | 14,003 | 22 rows × 8 — U.018–U.037 plus B1's 1963–64 openings gap and volume 1's cost/land-price/vendor gap; every High-importance row carries a follow-up route | 5 | WITHIN CAP |
| `decisions.csv` | 344 | 2,651 | 3 rows × 15 — volume 1's three reconstructable decisions; volume 2 and B1 emitted none (§N's structural hole is the finding) | 5 | WITHIN CAP |
| `validation.csv` | 232 | 1,858 | 4 rows × 11 — the four signals in volume 1's shared block | 5 | WITHIN CAP |
| `failures.csv` | 72 | 649 | 1 row × 11 — routed out of that same shared block by its own content, per §13 and volume 1's row-count paragraph | 5 | WITHIN CAP |
| `channels.csv` | 171 | 1,400 | 3 rows × 11 — §I's three tested channels | 5 | WITHIN CAP |
| `_parts/s1_p1.md` | 15,467 | 105,781 | superseded intermediate, carried untouched + one dated `MERGED` line | — | SUPERSEDED, retained as audit trail |
| `_parts/s1_p2.md` | 21,633 | 146,584 | superseded intermediate, same treatment | — | SUPERSEDED, retained as audit trail |

**Register-file geometry (§9.4):** no register approaches the 150,000-row / 40 MB split trigger, so no
continuation file exists and no multi-line field is split across files. `sources.csv` is the global-append-only
provenance register: ids are never redefined, and the ids retired by this merge are recorded as aliases rather
than deleted.

**Stage vocabulary (§13, fixed 2026-09-25):** `stage1` on all 157 rows of the nine registers — verified by a
per-file distinct-value scan, which returned `['stage1']` nine times. **Numeric stage values found and
normalised: 0**, so there is no normalisation count to report. Post-boundary `(PB)` rows (the FY1972–74 recap
legs) stay `stage1` because the emitting dossiers labelled them so and they are used only as consequences of
the stage, never as in-span observation; that reading is stated here rather than left implicit.

**Coverage of this pass:** 9 of 9 registers present at the company root (they existed nowhere before this
pass: not at the root, and not under `research/` either — `research/` holds the two probe dossiers and the
`## Register rows` block inside `B1_dayton_print_records.md`, which was applied as an emission, not left as a
dossier). Stage 2, Stage 3, `final_report.md`, `adversarial_review.md` and `context_appendices.md` do not exist
for this company yet and are outside this brief.

## Proof of non-destruction

| Check | Before | After | Account |
|---|---|---|---|
| narrative words | 36,954 across `_parts/s1_p1.md` + `_parts/s1_p2.md` | 37,507 in `stage_1.md` | −70 (the two per-part filename H1s and the two stale `SCAFFOLDED … nothing written yet` markers, dropped because a header that tells a cold reader the file is empty is an instruction-layer defect, §14 rule 10) + 607 (merge header and merge note) + 16 (the volume divider) = **exactly 37,507**; part bodies 15,361 + 21,523 are carried in full, asserted by the build |
| register rows on disk | 0 CSVs (191 rows emitted as text in two parts and one dossier) | **157 rows** | 191 requested − 34 folded into 28 collision groups − **0 refused**; every folded emission's distinct wording is retained inside the kept row |
| §U anchors | 37 declared (`ANCHORS: U.001-U.037` in volume 2) | 37 declared, in the merged volume, carried verbatim | `gates.py --checks anchors`: **37 narrative anchors ↔ 37 register anchors**, 0 undeclared, 0 uncovered; §U's 37 anchor entries are physically intact in the volume |
| part files | 2 | 2 | untouched but for one appended dated `MERGED` line each (+71 and +75 words); no line rewritten, nothing moved, nothing deleted |
| `research/` dossiers | 2 | 2 | read, applied, not modified; B1's 65 rows are now registers, and its rows' own `Q`-series numbering continues in `quantitative.csv` |
| `sources/` archive | 17 documents in 8 sub-directories | unchanged | read-only to this pass |
| other companies | — | unchanged | this pass wrote only inside `company_042_target/` plus its own QC sheet |

## Gate record

Before (2026-09-26, pre-merge): **1 finding** — `coverage / registers: no register CSVs at root or research/
-- csv/anchors gates DID NOT RUN`, plus `corrections: no CORRECTIONS.md -- gate DID NOT RUN` (recorded as
*DID NOT RUN*, not a pass), and `anchors: no register anchors found -- UNANSWERED, not passed`.

After (same checks `csv,keys,anchors,budget,corrections`): **0 findings, 19 passes** — `csv` 9/9 registers
width-clean with every `S####` token resolving, `keys` `stage_1.md` source tokens resolve, `anchors` citation
resolution + parity 37 ↔ 37, `budget` 37,507 < 60,000, `corrections` all 6 retractions reach registers and
volumes. Advisory notes, not defects: 6 prose mentions (`U.1`–`U.5`, the reserved `U.101`) that are subsection
numbers or a reserved range, not anchors.
